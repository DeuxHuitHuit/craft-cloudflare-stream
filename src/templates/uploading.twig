<p>Uploading <strong>{{ value.meta.name }}</strong> to your Cloudflare Stream account...</p>
<p>
    Status: {{ value.status.state }} {{ value.status.pctComplete|default(1) }}%
</p>
<p>{{ 'Refresh this page periodically to check if the video is ready'|t('cloudflare-stream') }}</p>
<small>Uid {{ value.uid }}</small>
